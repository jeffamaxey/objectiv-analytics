{
  "name": "root",
  "description": "Object Tracker monorepo root package",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "core/*",
    "plugins/*",
    "queues/*",
    "trackers/*",
    "transports/*"
  ],
  "engines": {
    "node": ">= 12",
    "npm": ">= 8.5"
  },
  "scripts": {
    "build": "turbo run build",
    "clean": "yarn exec make clean && yarn test --clearCache && yarn cache clean",
    "depcheck": "turbo run depcheck",
    "deploy": "shx rm -f .npmrc && turbo run deploy && changeset publish",
    "deploy:verdaccio": "shx cp ./verdaccio/.npmrc .npmrc && turbo run deploy && changeset publish && shx rm -f .npmrc",
    "list": "yarn workspaces list",
    "prettify": "turbo run prettify",
    "test": "turbo run test",
    "test:ci": "turbo run test:ci",
    "test:coverage": "turbo run test:coverage",
    "tsc": "turbo run tsc",
    "utils:generate": "yarn workspace @objectiv/utilities generate && yarn workspace @objectiv/developer-tools generate && yarn prettify && yarn tsc"
  },
  "devDependencies": {
    "@changesets/cli": "^2.24.0",
    "shx": "^0.3.4",
    "turbo": "^1.3.4"
  },
  "packageManager": "yarn@3.0.2"
}
